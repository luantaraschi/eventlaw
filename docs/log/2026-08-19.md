# Work log — 2026-08-19

## Context

The user approved the larger direction and asked to begin implementation while
preserving context across sessions.

## Work started

- Checked naming. `tracecheck` is free on npm but collides semantically with an
  active GitHub project for evaluating multi-step agent traces.
- Selected `eventlaw` provisionally. It was free on npm at check time and had no
  meaningful exact repository collision.
- Created an unpublished TypeScript package with `private: true`.
- Defined initial finite-trace semantics, portability constraints, validation
  gates, and a persistent session handoff.

## Evidence and decisions

- The existing `lull` property tests contain three good seed laws: progress,
  exclusion during takeover, and uniqueness/accounting.
- The first core will have no dependency on `lull` or `fast-check`; integrations
  remain outside the semantic engine.
- Function predicates were rejected in the initial matcher DSL because they
  cannot be serialized or executed outside the creating process.

## Open questions

- Whether field-path matchers remain readable for nested production schemas.
- Whether `complete: true` should fail every unresolved progress obligation or
  distinguish graceful source completion from truncated traces.
- What bounded-memory guarantees each temporal operator can provide online.

## Vertical slice completed

- Implemented a JSON-safe matcher AST with literals, captures, and references.
- Implemented three-valued finite-trace verification for progress, exclusion,
  and uniqueness laws.
- Added partitioning, visible vacuity, decreasing-time rejection, trace
  minimization, and readable timeline output.
- Ported three `lull` laws and tested them against `@luantaraschi/lull/core`.
- Corrected the takeover adapter model so lazy TTL expiry emits
  `takeover.ended`; otherwise the law would report legitimate post-expiry turns.
- Added an experimental async monitor using the same law AST.
- Added an optional `fast-check` subpath. With seed 42 it reduced a faulty runtime
  to commands `["takeover", "tick"]` and a two-event trace.
- Added two runnable examples.

## Verification snapshot

- 23 tests in 6 files pass.
- TypeScript strict typecheck passes.
- ESM, CJS, and declaration builds pass.
- Prettier check passes.
- `npm audit --audit-level=low` reports zero vulnerabilities after overriding a
  vulnerable transitive `esbuild` development version.
- `npm pack --dry-run`: 19 files, 25.2 kB compressed, 162.7 kB unpacked.
- `npm ls --omit=dev`: no required runtime dependencies.

## Honest limitations

The streaming monitor is a semantic proof, not production-ready: it retains the
complete trace and is quadratic over a growing stream. It now has an explicit
clock, rejects backward events, and keeps observed failures terminal. The next
engineering milestone is bounded, incremental per-operator state.

## Session continuity

Added workspace and project `AGENTS.md` files. Future sessions are instructed to
read the portfolio notebook, project handoff, decisions, semantic spec, and latest
work log before editing. Publication and remote creation remain explicitly out of
scope without user approval.

## Repository bootstrap approved

- The user explicitly approved creating the repository to organize the work.
- The repository is private during product validation; making it public remains
  a separate decision.
- The remote must be created empty, without GitHub-generated starter files,
  because those options would create a commit.
- Authorship rule: Codex never creates or amends commits, directly or through a
  tool/API. Only Luan stages and commits. Codex leaves a verified working tree
  and may suggest a message.
- Added a minimal GitHub Actions workflow for Node 20 and 22 plus a contribution
  guide. They remain local until Luan makes and pushes the initial commit.
- Created `https://github.com/luantaraschi/eventlaw` as a private, empty
  repository; initialized local `main` and configured the HTTPS `origin`.
- Added repository and issue-tracker metadata to `package.json`.
- Verification after creation: local `HEAD` does not exist, `git ls-remote
origin` returns no refs, and every working-tree file remains untracked. No
  staging, commit, or push was performed.
- Local verification still passes: 23 tests, strict typecheck, ESM/CJS/type
  builds, Prettier, and npm audit with zero known vulnerabilities.

## Incremental monitor completed

- Replaced full-trace storage and repeated `verifyTrace` calls with per-law,
  per-partition state.
- Progress retains unmatched triggers, captures, deadlines, and evidence indexes;
  satisfied and expired obligations are released.
- Exclusion retains only the latest open start per partition and releases it on
  the matching end.
- Exact uniqueness retains all distinct keys and is explicitly classified as
  unbounded rather than being marketed as bounded.
- Added `monitoringProfile(laws)` for static classification and
  `monitor.stats()` for runtime retained-entry counts.
- Removed the experimental `snapshot()` method because returning the complete
  trace would require retaining the history the new monitor intentionally drops.
- Preserved deadline semantics: a consequence exactly at the deadline passes;
  late consequences include trigger and consequent indexes; explicit time and
  terminal failures remain intact.

## Incremental verification

- Added deterministic tests for exact deadlines, late evidence, completion,
  operator retention, state release, and absence of unrelated history retention.
- Added property-based differential tests for all three operators. Across 750
  generated traces, each online prefix matched finite verification until the
  first terminal failure.
- Full result: 34 tests in 7 files, strict typecheck, ESM/CJS/declaration builds,
  Prettier, and npm audit with zero vulnerabilities.
- `npm pack --dry-run`: 19 files, 41.4 kB compressed, 236.3 kB unpacked.
- Final Git audit: zero staged files, no local `HEAD`, and zero remote refs. No
  commit or push was performed; the private GitHub repository remains empty.

## Remaining risks after incrementalization

- `atMostOnce` still needs an explicit window, scope end, or external state store
  before it can offer bounded retention.
- Progress deadline expiration currently scans every open obligation on each
  event or clock advance. This removes historical replay but can still be costly
  under high trigger volume; benchmark before adding a heap/index.
- Memory stats count logical retained entries, not byte size.

## Explicit uniqueness retention completed

- Added a serializable retention node to every `atMostOnce` law. Existing
  `.per(...)` and `.perEach(...)` definitions use `forever` explicitly.
- Added `.within(d)` for inclusive sliding-window uniqueness. A key is released
  only after the boundary; an occurrence outside the window becomes the new
  reference.
- Added `.resetOn(event)` for business scopes. The reset clears only its
  partition and runs before target matching on the same event.
- Static profiles now classify lifetime uniqueness as `unbounded`, windowed
  uniqueness as `window-bounded`, and reset uniqueness as `scope-bounded`.
- Online clocks release expired window keys even when no domain event arrives.
- Added deterministic boundary, sliding-reference, partition isolation,
  same-event reset, validation, serialization, profile, and retained-state tests.
- Added 500 generated differential traces for windowed and reset uniqueness,
  bringing the differential total to 1,250 traces.
- Added `examples/webhooks.ts`, a runnable non-agent example for delivery
  deduplication across a provider retry horizon. It shows the failing duplicate,
  retained-state expiration, and allowed reuse after the window.
- Verification before the final documentation pass: 44 tests in 8 files,
  typecheck, ESM/CJS/declaration builds, Prettier, and audit all pass.
- All three runnable examples pass. `npm pack --dry-run` reports 19 files,
  47.3 kB compressed and 260.1 kB unpacked; required runtime dependencies remain
  empty.
- Final authorship audit remains clean: no staged files, no local `HEAD`, zero
  remote refs, and no tarball written by the dry run. No commit or push occurred.

## Open-source presentation and initial push authorized

- Luan explicitly authorized a one-time exception for Codex to create the first
  commit and push `main`. Git author and committer remain `Luan Taraschi`; the
  standing no-commit rule resumes immediately afterward.
- Rebuilt the README using the strongest parts of `lull`'s structure: problem
  first, concrete behavior and output, reproducible proof, scope boundaries,
  honest limitations, commands, tests, and clear contribution paths.
- Added CI/status/dependency/license badges without adding an npm badge because
  the package is intentionally unpublished.
- Added a Mermaid map showing one serializable law feeding generated tests,
  recorded traces, and the live monitor.
- Expanded the contribution guide with command reference, bug and law-proposal
  formats, PR evidence, project constraints, and release status.
- Added `CHANGELOG.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, issue forms, and a
  pull request template.
- Added package author and discovery keywords. Publication remains out of scope.

## Progress deadline benchmark and index

- Measured the pre-change monitor with long-lived progress obligations in one
  partition. Five-sample medians were 7.44 ms for 1,000, 18.10 ms for 2,000, and
  76.27 ms for 4,000 opens. The last doubling cost 4.2× and confirmed quadratic
  behavior.
- Replaced arrays and full pending scans with a set per partition and a linked
  list ordered by deadline. Monotonic event time plus fixed `withinMs` keeps
  append order valid.
- Satisfaction unlinks its obligation directly. Expiration walks only the
  reached deadline prefix. Events with a different type from the consequent skip
  binding scans.
- Added `npm run bench:progress` and `docs/benchmarks.md` with environment,
  methodology, baseline, indexed results, and limits on interpretation.
- The first recorded reproducible indexed run measured medians of 1.24, 1.03,
  1.07, 2.49, and 4.60 ms for 1k, 2k, 4k, 8k, and 16k opens respectively.
- Later full-check reruns kept the same shape, with the 16k case between 4.56
  and 4.95 ms; the benchmark remains a local microbenchmark, not a universal
  throughput claim.
- Added a 10,000-obligation satisfaction test. All 1,250 generated differential
  traces still agree between online and finite engines.

## Maintained Node and Actions baseline

- The official Node.js lifecycle page lists Node 20 as EOL since 2026-03-24.
  Raised `engines.node` and the build target to Node 22; prepared CI for Node 22
  and 24.
- GitHub's official release APIs reported `actions/checkout@v7.0.1` and
  `actions/setup-node@v7.0.0` as current. The workflow now uses their v7 major
  tags, removing the initial CI's deprecated action-runtime warning.
- Full local result: 45 tests in 8 files, strict typecheck, Node 22 ESM/CJS/type
  builds, Prettier, audit with zero vulnerabilities, and the benchmark.
- `npm pack --dry-run`: 20 files, 51.2 kB compressed, 272.2 kB unpacked.
- These changes are intentionally unstaged and uncommitted for Luan.

## Performance commit explicitly authorized

- Luan explicitly authorized committing the prepared performance/CI milestone
  and continuing to the next steps.
- Created local commit `a3ea299` (`perf: index progress deadlines`) with
  `Luan Taraschi` as author and committer.
- No push was requested or performed. Local `main` is one commit ahead of
  `origin/main`; the standing no-commit rule resumed after this commit.

## External validation made repeatable

- Added `docs/validation.md` with two uncoached TypeScript comprehension sessions
  and one webhook-operator session.
- The gates measure explanation time, semantic predictions, ability to write a
  law, and identification of partition, key, clock, retention, and restart
  requirements.
- Added anonymized recording fields and decision rules. No external sessions
  have occurred yet, so no validation gate was marked complete.

## First trace adapter selected and implemented

- Compared JSON Lines, OpenTelemetry events, Kafka, and test-runner reporters in
  `docs/adapters.md`.
- Selected JSON Lines first because it exercises recorded-trace verification,
  works with files and pipelines, and adds no vendor dependency. OpenTelemetry
  remains the leading second candidate after a real record resolves nanosecond
  timestamps, event names, attributes, and resource mapping.
- Added the experimental `eventlaw/jsonl` package subpath with `parseJsonl`,
  incremental `decodeJsonl`, collecting `readJsonl`, and `JsonlTraceError`.
- String and UTF-8 byte chunks are supported, including byte chunks split inside
  a multibyte character. Blank lines, BOMs, malformed JSON, non-object values,
  empty event types, and invalid timestamps fail with source and one-based line.
- Added a runnable JSONL webhook trace and an adapter-proposal issue form focused
  on mapping, ordering, replay, clocks, dependencies, and failure behavior.
- Deferred durable lifetime uniqueness to a store-specific adapter. Atomic
  check-and-record, namespacing, idempotency, concurrency, and store failures must
  be learned before the synchronous core is widened.

## JSONL verification

- 56 tests in 9 files pass, including 11 JSONL adapter tests and the existing
  1,250 differential traces.
- Strict typecheck and Node 22 ESM/CJS/declaration builds pass for the core,
  `fast-check`, and `jsonl` entries.
- The JSONL webhook example reports the expected duplicate with a two-event
  timeline.
- Prettier passes and `npm audit --audit-level=low` reports zero vulnerabilities.
- `npm pack --dry-run`: 26 files, 56.9 kB compressed, 296.1 kB unpacked.
- All JSONL and validation changes remain unstaged and uncommitted for Luan.

## JSONL commit explicitly authorized

- Luan explicitly authorized committing the JSONL/validation milestone and
  starting the next step.
- Created local commit `360e851` (`feat: add jsonl trace adapter`) with
  `Luan Taraschi` as author and committer.
- No push was requested or performed. Local `main` is two commits ahead of
  `origin/main`; the standing no-commit rule resumed after this commit.

## External trace boundary: official OTLP/JSON Event

- No JSONL or NDJSON trace outside `eventlaw` existed in the workspace.
- Selected OpenTelemetry Protocol's official `examples/events.json`, pinned at
  upstream commit `b5947908941290bfa11cec2abf714e700412b5d7`. The unchanged
  Apache-2.0 fixture and attribution live under `tests/fixtures/` and are excluded
  from the npm package.
- The generic JSONL adapter rejected the pretty-printed batch at line 1, as
  intended. Inspection exposed the required mapping: nested resource/scope/log
  batches, `eventName`, uint64 nanosecond strings, dotted semantic attributes,
  AnyValue, and a body-owned `type` field.

## Structural OpenTelemetry adapter

- Added the dependency-free `eventlaw/opentelemetry` subpath for an
  already-decoded OTLP/JSON `ExportLogsServiceRequest`.
- Only named LogRecord Events become `TraceEvent`; ordinary logs are counted in
  `skippedLogRecords` so vacuous traces are not caused silently.
- `timeUnixNano` is preferred over `observedTimeUnixNano`. Integer milliseconds
  drive `at`; exact decimal nanoseconds remain in `otel` for audit and future
  precision decisions.
- Body, record attributes, resource, and instrumentation scope are namespaced.
  Dotted semantic attribute keys become nested objects compatible with matcher
  paths; prefix collisions fail instead of overwriting data.
- AnyValue strings, booleans, doubles, arrays, key-value lists, bytes, and int64
  values are decoded. Int64 values outside JavaScript's safe range remain exact
  decimal strings.
- Request order is preserved. No SDK, protobuf decoder, HTTP receiver, Collector
  connection, authentication, compression, backpressure, or cross-request order
  was added.

## OpenTelemetry verification

- 64 tests in 10 files pass, including 8 adapter tests, the unchanged official
  fixture, and the existing 1,250 differential traces.
- Strict typecheck and Node 22 ESM/CJS/declaration builds pass for all four
  package entries.
- Prettier passes and `npm audit --audit-level=low` reports zero vulnerabilities.
- `npm pack --dry-run`: 32 files, 66.5 kB compressed, 356.1 kB unpacked.
- All OpenTelemetry changes remain unstaged and uncommitted for Luan.

## OTLP adapter commit explicitly authorized

- Luan explicitly authorized committing the prepared structural OTLP adapter
  while the second-payload validation was starting.
- Created local commit `b0c225e` (`feat: add otlp json event adapter`) with
  `Luan Taraschi` as author and committer.
- No push was requested or performed. Local `main` is three commits ahead of
  `origin/main`; the standing no-commit rule resumed after this commit.

## JavaScript SDK export captured

- Public repository search did not produce a second complete named Event batch.
  Created an isolated project under the system temporary directory instead.
- Used exact official package versions: `@opentelemetry/api-logs@0.221.0`,
  `@opentelemetry/sdk-logs@0.221.0`,
  `@opentelemetry/exporter-logs-otlp-http@0.221.0`,
  `@opentelemetry/resources@2.10.0`, and
  `@opentelemetry/semantic-conventions@1.38.0`.
- Two loggers emitted deterministic `order.accepted` and `order.shipped` Events
  from different instrumentation scopes plus one ordinary log. A local HTTP
  server captured the exporter's `application/json` request body.
- The payload exercised nested AnyValue bodies, arrays, numeric intValue,
  three-level dotted attributes, resource attributes, multiple scopes, skipped
  logs, source/observed timestamps, and a cross-event capture/ref law.
- OpenTelemetry packages remain absent from `eventlaw` dependencies.

## SDK incompatibility found and fixed

- The JavaScript exporter serialized the bodyless `order.shipped` Event as
  `body: {}`. The first adapter version rejected it because it contained no
  recognized AnyValue variant.
- Empty AnyValue objects now mean an absent body and are omitted from the
  `TraceEvent`. Non-empty malformed AnyValues remain errors.
- Added the deterministic SDK payload as a regression fixture with package
  versions and capture method documented in `tests/fixtures/README.md`.
- The converted batch contains two Events, visibly reports one skipped ordinary
  log, and passes the correlated six-second shipping law.
- 10 focused OpenTelemetry tests and 66 total tests in 10 files pass, including
  the existing 1,250 differential traces.
- Strict typecheck, ESM/CJS/declaration builds for all four entries, Prettier, and
  audit with zero vulnerabilities pass.
- `npm pack --dry-run`: 32 files, 66.7 kB compressed, 357.1 kB unpacked.
- The captured fixture was regenerated and compared structurally with the stored
  copy; both serialized to the same 1,497-character canonical JSON.
- Cleanup of the isolated system-temp project was attempted only after resolving
  and validating its exact path, but recursive deletion was blocked by the
  environment policy. It contains only the temporary package and dependencies,
  outside the repository.
- All SDK-compatibility changes remain unstaged and uncommitted for Luan.

## SDK-compatibility commit explicitly authorized

- Luan explicitly authorized committing the SDK compatibility fix and starting
  Collector validation.
- Created local commit `3433993` (`fix: accept empty otlp event bodies`) with
  `Luan Taraschi` as author and committer.
- No push was requested or performed. Local `main` is four commits ahead of
  `origin/main`; the standing no-commit rule resumed after this commit.

## Collector round trip

- Docker Desktop was available locally. Pulled the official stable
  `otel/opentelemetry-collector:0.157.0` image at digest
  `sha256:4019ce4d7e7791a1a255fffb2f407af66d5017cc65543469ba565c4f47f795b8`.
- Ran an isolated logs pipeline: OTLP/HTTP receiver, no processors, and stable
  `otlp_http` exporter with JSON encoding, compression disabled, and retries
  disabled. The target was a local capture server.
- Sent the deterministic JavaScript SDK batch through the Collector. It preserved
  Events, timestamps, bodies, attributes, resources, scopes, and ordering.
- Collector JSON omitted zero-value `droppedAttributesCount` fields and empty
  attribute arrays. It canonicalized numeric `intValue: 0` to string `"0"`.
- The adapter already handles both representations. Added the captured output as
  a fixture and proved its conversion deep-equal to the direct SDK conversion.
- No source change or Collector-specific branch was needed.
- The validation container was stopped and removed after capture.
- 11 focused OpenTelemetry tests and 67 total tests in 10 files pass, including
  the existing 1,250 differential traces.
- Strict typecheck, ESM/CJS/declaration builds for all four entries, Prettier, and
  audit with zero vulnerabilities pass.
- `npm pack --dry-run`: 32 files, 66.8 kB compressed, 357.3 kB unpacked.
- All Collector fixture and documentation changes remain unstaged and
  uncommitted for Luan.

## Multi-resource application-shaped export

- Reused the isolated official JavaScript SDK project and made
  `@opentelemetry/api@1.9.1` an explicit temporary dependency.
- Created independent `checkout-api` and `fulfillment-worker` logger providers,
  each with its own resource attributes and OTLP/HTTP exporter.
- Emitted deterministic `order.accepted` and `order.shipped` Events with the
  same valid W3C trace ID, different span IDs, and sampled trace flags.
- Collector 0.157.0 used a batch processor with `send_batch_size: 2` and emitted
  one JSON request containing both resource groups. The container was stopped
  and removed after capture.
- Added the captured request as a fourth fixture. A cross-service law captures
  order ID plus trace ID, passes on the original trace, and fails when only the
  consequence trace ID changes.
- The capture exposed `flags: 1`. The adapter now preserves it as `otel.flags`
  and rejects non-uint32 flag values rather than coercing them.
- 13 focused OpenTelemetry tests and 69 total tests in 10 files pass, including
  the existing 1,250 generated differential traces.
- Strict typecheck, ESM/CJS/declaration builds, Prettier, and audit with zero
  vulnerabilities pass.
- `npm pack --dry-run`: 32 files, 67.3 kB compressed, 360.2 kB unpacked.
- The harness is project-generated evidence; an anonymized external application
  payload and the three planned human sessions remain open.
- All multi-resource fixture, adapter, test, and documentation changes form the
  reviewed Collector milestone for Luan.

## Collector milestone publication authorized

- Luan explicitly authorized committing the complete current progress and
  pushing local `main` directly to GitHub.
- The authorized snapshot includes Collector normalization, the two-resource
  trace capture, `otel.flags`, all regression tests, and their documentation.
- Git author and committer remain `Luan Taraschi`; no PR or alternate branch is
  created because direct publication of the current progress was requested.
- The exception is consumed by this milestone commit and push; the standing
  no-commit rule resumes immediately afterward.

## README visual identity draft

- Reviewed three user references: expressive wordmark plus badges, restrained
  editorial mark, and product logo plus one-line promise.
- Chose a product-specific direction instead of borrowing their identities: a
  trigger and consequence connected under a visible `≤ 5s` temporal law.
- Created matching light and dark SVG heroes with no external fonts, scripts,
  tracking, or bitmap-rendered text.
- Reorganized the README top around the hero, five factual badges, short section
  navigation, and the existing honest research-preview notice.
- Documented palette roles, typography, hierarchy, accessibility, asset rules,
  and possible avatar/social-preview extensions in `docs/brand.md`.
- Rendered both SVGs to temporary PNG previews for visual QA. The first pass
  exposed overlapping consequence/deadline labels; their alignment was fixed.
- The complete visual draft remains unstaged and uncommitted for Luan's review.

## README wordmark revision

- Luan rejected the diagrammatic hero and clarified that the display should
  contain only the project name.
- Removed the icon, tagline, timeline, status, deadline, mode labels, container,
  and colored background from both SVGs.
- The revised mark is centered lowercase `eventlaw` on a transparent canvas.
  `event` follows theme ink and `law` uses the only gradient in the asset.
- Updated the brand guide, decision record, handoff, changelog, and image alt text
  so no documentation still presents the diagram as the chosen direction.
- The revised wordmark remains unstaged and uncommitted for visual review.

## Wordmark approved and exported with alpha

- Luan approved the name-only direction and requested a background-free PNG.
- Exported light and dark PNGs directly from the SVG sources at 1280 × 280.
- Both files report four channels and `hasAlpha: true`; no background color was
  flattened into either asset.
- Updated the README `<picture>` to use the PNGs while preserving SVG sources for
  future edits.
- Added `assets` to the npm package allowlist so its bundled README keeps working
  when publication is eventually enabled.
- `npm pack --dry-run`: 36 files, 110.2 kB compressed, 405.6 kB unpacked; no
  tarball was written.
- The approved visual work remains unstaged and uncommitted for Luan.

## Visual identity publication authorized

- Luan asked to push the approved wordmark and transparent PNG milestone.
- The authorization covers one commit of the reviewed visual working tree and a
  direct push to `origin/main` with Luan as author and committer.
- No PR or alternate branch is created because direct repository publication was
  requested.
- The exception is consumed by this commit and push; D-010 resumes immediately
  afterward.
