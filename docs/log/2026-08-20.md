# Work log — 2026-08-20

## Dogfood before external sessions

- **Decision:** Luan deferred the two TypeScript comprehension sessions and the
  webhook-operator session until Eventlaw is first exercised in his own projects.
- **Scope:** `lull` is the first consumer; one additional owned event-driven
  project supplies an independent domain and trace.
- **Protocol:** `docs/dogfood.md` requires installation from the packed artifact,
  at least three genuine laws, and coverage of generated, recorded, and live
  execution across the two consumers.
- **Boundary:** this is integration evidence, not external comprehension
  evidence. Existing human gates remain open and publication remains blocked.
- **Authorship:** Luan replaced the blanket prohibition on Codex-created commits.
  Git operations are now allowed only for an explicitly authorized scope, using
  his configured identity and without Codex attribution.
- **Authorization:** the complete reviewed beta-readiness and dogfood-planning
  tree may be committed and pushed directly to `origin/main` in this session.
- **Next action:** start dogfood without importing Eventlaw source files directly.

## Verification

- `npm run release:check` passed after the dogfood documentation change:
  formatting, strict typecheck, 69 tests in 10 files, ESM/CJS/declarations,
  clean-consumer package smoke, and audit with zero vulnerabilities.
- `git diff --check` passed. The staging area is empty.
- Local `HEAD` and `origin/main` both resolve to `d030cb5`; the private branch is
  synchronized before Luan's preparation commit.
- `package.json` still reports `private: true` and version `0.1.0-beta.1`.

## Beta-readiness publication

- The workspace and project `AGENTS.md` files now persist Luan's explicit
  delegation model: Git mutations are opt-in per scope instead of permanently
  prohibited.
- This milestone publishes only repository preparation. It does not make the
  repository public, publish npm, create a release tag, or announce the project.
